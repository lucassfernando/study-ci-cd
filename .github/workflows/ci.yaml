name: ci-python-workflow
on: [push]
jobs: 
  check-application:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@4
        with: 
          python-version: '3.12'
      - run: pytest
      
